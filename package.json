{
  "name": "plane-cli",
  "version": "0.2.2",
  "description": "Machine-friendly TypeScript CLI for Plane.so.",
  "keywords": [
    "agents",
    "automation",
    "cli",
    "llm",
    "plane",
    "typescript"
  ],
  "license": "MIT",
  "bin": {
    "plane-cli": "./bin/plane-cli",
    "plane-mcp": "./dist/mcp/index.js"
  },
  "files": [
    "bin",
    "dist",
    "README.md"
  ],
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts src/mcp/index.ts --format esm --dts --clean --out-dir dist && bun build src/index.ts --compile --outfile dist/plane-cli",
    "ci": "bun run test && bun run build && bun run lint",
    "dev": "tsx src/index.ts",
    "dev:mcp": "tsx src/mcp/index.ts",
    "lint": "oxlint .",
    "prepack": "bun run build",
    "format": "oxfmt --write .",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "redis": "^6.1.0",
    "yaml": "^2.7.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "oxfmt": "^0.54.0",
    "oxlint": "^1.0.0",
    "tsup": "^8.3.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
